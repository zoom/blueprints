{
  "name": "zoom-blueprints",
  "version": "0.1.0",
  "private": true,
  "description": "Curated, use-case-driven reference implementations for the Zoom developer platform.",
  "engines": { "node": ">=20" },
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "node --test scripts/"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
